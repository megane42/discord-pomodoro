# frozen_string_literal: true

RSpec.describe Discord::Pomodoro do
  it "has a version number" do
    expect(Discord::Pomodoro::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
